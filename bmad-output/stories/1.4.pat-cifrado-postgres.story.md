<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories
  File name: 1.4.pat-cifrado-postgres.story.md

  This file is SELF-CONTAINED (~8K-token target): a dev agent should need nothing else.
  It is the LAST planning artifact before external dev handoff.

  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — they are the compiled, cited contract.
  Only this planning skill may change a LOCKED section, via an explicit Update + decision-log entry.

  The external dev tool fills ONLY the "Dev Agent Record" section.
-->

# 1.4: PAT cifrado en Postgres vinculado a chat_id

**Story ID:** 1.4
**Epic:** 1 — Autenticación y multi-tenancy
**Slug:** pat-cifrado-postgres
**Status:** done   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **desarrollador**,
I want **guardar el PAT de cada usuario cifrado en reposo en Postgres, vinculado a su `chat_id` de Telegram**,
so that **ningún dato sensible (la credencial que da acceso a los datos financieros de un usuario) quede en texto plano en la base de datos, cumpliendo el NFR de seguridad más crítico del sistema**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. La tabla `usuarios_autorizados` (creada en 1.1) se extiende con una columna `pat_cifrado` (y los metadatos de cifrado que hagan falta, ej. IV/nonce si el algoritmo lo requiere) vía una nueva migración — sin modificar ni recrear la migración original de 1.1.
2. Dado un PAT nuevo (entregado por el flujo de alta de 1.3), cuando se guarda, entonces en la base de datos aparece cifrado — una inspección directa de la fila en Postgres nunca muestra el PAT en texto plano.
3. Existe una función de descifrado que, dado un `chat_id`, retorna el PAT en texto plano únicamente en memoria, para uso inmediato por la capa que lo necesite (historia 1.5) — nunca se escribe a disco ni se loguea el resultado descifrado.
4. La clave de cifrado vive fuera del repositorio y fuera de la propia base de datos (variable de entorno / secreto gestionado), de forma que tener acceso de lectura a Postgres no sea suficiente por sí solo para recuperar los PAT.
5. Ningún log de la aplicación (incluyendo logs de error/stack traces) puede contener el PAT en texto plano, cifrado o no — se aplican los mismos criterios de higiene de logs definidos en la Definition of Done del proyecto.

<!-- Keep to ~3-7. More than ~7 means the story is too big — split it. -->

## Tasks / Subtasks

- [ ] Crear migración que agrega la columna `pat_cifrado` (y metadatos de cifrado necesarios) a `usuarios_autorizados` (AC: #1)
- [ ] Implementar módulo de cifrado/descifrado simétrico (algoritmo autenticado, ej. AES-256-GCM vía el módulo `crypto` nativo de Node — no hay una elección explícita en la arquitectura, ver Dev Notes) (AC: #2, #3)
- [ ] Cargar la clave de cifrado desde variable de entorno / gestor de secretos, con validación de arranque (el servicio no debe iniciar si falta la clave) (AC: #4)
- [ ] Extender el repositorio `usuarios.repository.ts` (de 1.1) con `guardarPatCifrado(chatId, pat)` y `obtenerPatDescifrado(chatId)` (AC: #2, #3)
- [ ] Auditar los puntos de logging existentes (whitelist de 1.1, flujo de alta de 1.3) para confirmar que ninguno accidentalmente serializa el objeto de usuario completo (que ahora incluye `pat_cifrado`) (AC: #5)

## Dev Notes

<!-- LOCKED. Concrete guidance compiled from the planning docs WITH SOURCE CITATIONS.
     Cite every fact: [Source: prd.md#anchor] / [Source: architecture.md#anchor] /
     [Source: ux-design.md#anchor]. Label your own inferences [Inference]. -->

- Cambio explícito de v1 a v2: "Usuarios + PAT: v1 (1 usuario) → Tabla simple en SQLite | v2 (50 usuarios) → **Postgres**, con el PAT **cifrado en reposo** (no en texto plano)." Esta historia implementa exactamente esa fila de la tabla de cambios. [Source: docs/plan/system-design-v2-multiusuario.md#4-cambios-en-el-bot-service]
- Requisito equivalente (HU-08): "Como desarrollador, quiero guardar el PAT de cada usuario cifrado en Postgres vinculado a su `chat_id` de Telegram, para que ningún dato sensible quede en texto plano. Dado un PAT nuevo, cuando se guarda, entonces en la base de datos aparece cifrado, no en texto plano." Prioridad 🔴, 5 pts. [Source: docs/plan/backlog-scrum-completo.md#épica-1-autenticación-y-multi-tenancy]
- Esta historia es una pieza directa del NFR más importante del proyecto: "Aislamiento estricto: el usuario A jamás debe poder ver ni un byte de los datos del usuario B. Esto pasa de 'no aplica' a el requisito no funcional más importante del sistema." El cifrado del PAT es la defensa contra que una fuga de la base de datos (backup mal protegido, acceso no autorizado a Postgres) se traduzca directamente en acceso a los datos financieros de todos los usuarios. [Source: docs/plan/system-design-v2-multiusuario.md#1-requisitos-actualizados]
- La Definition of Done del proyecto es explícita y nombra el PAT directamente: "Si toca datos sensibles (PAT, credenciales), quedaron cifrados/fuera de logs." Esta historia es, literalmente, el cumplimiento de esa cláusula del DoD para el activo más sensible del sistema. [Source: docs/plan/backlog-scrum-completo.md#4-definition-of-done-dod]
- La arquitectura **no especifica** el algoritmo de cifrado concreto ni el mecanismo de gestión de la clave — solo exige que el PAT quede "cifrado en reposo". La elección de AES-256-GCM (autenticado, con nonce por fila) vía el módulo `crypto` nativo de Node, con la clave en una variable de entorno (`PAT_ENCRYPTION_KEY`) inyectada por Docker Compose/el gestor de secretos del host, es una decisión de implementación razonable dado el stack Node/TS + Docker Compose ya elegido, pero no está fijada por ningún documento fuente. [Inference]
- Esta historia **no** decide cómo rotar la clave de cifrado ni cómo re-cifrar PAT existentes ante una rotación — eso corresponde a la historia de rotación de secretos de Epic 8 (HU-33). [Inference — basado en que Epic 8 posee explícitamente "rotación y protección de secretos" como su alcance]

## Testing

<!-- LOCKED. STRATEGY ONLY — what to verify and how. NEVER run tests or quote coverage here. -->

- **Approach:** Unitario para el módulo de cifrado/descifrado (round-trip con distintos PAT de prueba); integración con Postgres de prueba para confirmar que la columna almacenada nunca coincide con el PAT en texto plano; revisión estática/test de humo de los puntos de logging para confirmar que no serializan el campo `pat_cifrado` ni el PAT descifrado.
- **Key scenarios:** cifrar y luego descifrar un PAT devuelve el valor original exacto; dos cifrados del mismo PAT (en momentos distintos) producen ciphertexts distintos si el algoritmo usa nonce aleatorio (evita que un atacante compare valores cifrados entre usuarios); el servicio falla al arrancar si `PAT_ENCRYPTION_KEY` no está configurada; una fila leída directamente de Postgres (sin pasar por la capa de descifrado) no contiene el PAT en texto plano bajo ninguna inspección.
- **Per-AC verification:** AC #1 → test de migración confirma que la columna existe con el tipo esperado y que la migración de 1.1 no fue alterada; AC #2 → test de integración que guarda un PAT de prueba y lee la fila cruda de Postgres, confirmando que el valor no es igual (ni contiene como substring) el PAT original; AC #3 → test de round-trip cifrar→descifrar, y test de que la función de descifrado no escribe a ningún log/archivo; AC #4 → test de arranque que confirma que el servicio falla explícitamente (no silenciosamente) si falta la clave; AC #5 → inspección/test de los logger calls existentes de 1.1 y 1.3, confirmando exclusión explícita de `pat_cifrado` y del PAT descifrado de cualquier objeto logueado.
- **Fixtures / mocks:** PAT de prueba (string dummy, nunca un PAT real); Postgres de prueba (contenedor efímero) para el camino de integración; variable de entorno de clave de cifrado configurada solo en el entorno de test.
- **Edge cases to cover:** PAT vacío o con caracteres especiales; clave de cifrado con longitud incorrecta para el algoritmo elegido; intento de descifrar con la clave equivocada (debe fallar de forma segura, no devolver basura silenciosamente — importante si el algoritmo es autenticado como GCM); `chat_id` sin PAT asociado todavía (usuario dado de alta en Firefly pero el paso de 1.3 aún no completó la entrega del PAT).

## Dependency Maps

- **Blocked by:** 1.1 (necesita la tabla `usuarios_autorizados` para extenderla), 1.3 (necesita el PAT en tránsito que produce el flujo de alta para tener algo que cifrar en el camino de integración — el módulo de cifrado en sí puede construirse y testearse de forma aislada con un PAT dummy en paralelo a 1.3, pero la integración completa depende de 1.3). [Inference]
- **Blocks:** 1.5 (necesita poder leer/descifrar el PAT correcto por `chat_id`); Epic 4 (el MCP multi-tenant depende de que exista un PAT descifrable por usuario).
- **External:** Postgres (Epic 0); mecanismo de gestión de secretos del host/Docker Compose para `PAT_ENCRYPTION_KEY`.

## Owned File/Module Scope

<!-- EXPLICIT list of paths this story may create or modify. This is the lever for
     parallel-conflict-free scheduling: disjoint scopes can run in parallel. Be path-precise,
     prefer files over broad globs, include test paths, and call out shared/contended files.
     Validated by ${CLAUDE_PLUGIN_ROOT}/scripts/scope-conflict-check.sh. -->

- `bot-service/src/db/migrations/0002_add_pat_cifrado_column.sql`
- `bot-service/src/auth/pat-crypto.ts` (cifrado/descifrado)
- `bot-service/test/auth/pat-crypto.test.ts`
- `bot-service/test/db/usuarios.repository.pat.test.ts`
- Shared/contended (must serialize if another story also lists it): `bot-service/src/db/usuarios.repository.ts` (extiende el repositorio creado en 1.1 y usado por 1.3 — coordinar nombres de métodos con ambas); `bot-service/.env.example` / configuración de secretos (agrega `PAT_ENCRYPTION_KEY`, también tocado por 1.3 para el token admin de Firefly III).

## Learnings from Previous Stories

<!-- Carried forward from completed sibling stories: chosen patterns, gotchas, helpers
     created, naming conventions. Only what is actionable for THIS story. -->

- El repositorio `usuarios.repository.ts` y la tabla `usuarios_autorizados` ya existen desde 1.1 con `chat_id` y `activo` — esta historia debe extenderlos con una migración nueva, no recrearlos (from 1.1).
- El flujo de alta de 1.3 es quien produce el PAT en tránsito que esta historia debe cifrar y persistir — confirmar la forma exacta en que 1.3 lo entrega (parámetro de función vs. evento) antes de fijar la firma de `guardarPatCifrado` (from 1.3).

## Dev Agent Record

<!-- LEFT EMPTY by planning. The external dev tool fills this in during implementation. -->

### Implementación

- **Algoritmo:** AES-256-GCM vía `node:crypto` nativo (cifrado autenticado,
  nonce de 12 bytes por operación, sin dependencias externas), confirmando la
  elección [Inference] de los Dev Notes.
- **Formato almacenado** (`pat_cifrado`, columna `TEXT` nueva de la migración
  `0002`): `"<iv-base64>.<authTag-base64>.<ciphertext-base64>"` — un solo
  campo cubre los metadatos de cifrado que exige GCM, sin columnas extra. AC #1.
- **Archivos nuevos:**
  - `bot-service/src/db/migrations/0002_add_pat_cifrado_column.sql` — `ALTER
    TABLE ... ADD COLUMN IF NOT EXISTS pat_cifrado TEXT`, no toca la migración
    de 1.1.
  - `bot-service/src/auth/pat-crypto.ts` — `cifrarPat`, `descifrarPat`,
    `decodificarClaveCifrado`, `PatCryptoError`. Funciones puras (reciben la
    clave como `Buffer`, no leen `env` internamente) para que sean
    testeables de forma aislada.
  - `bot-service/tests/auth/pat-crypto.test.ts` (11 tests) y
    `bot-service/tests/db/usuarios.repository.pat.test.ts` (8 tests, 3
    integración con Postgres real vía contenedor efímero — mismo patrón que
    1.1, se saltan sin Docker).
- **Archivos modificados:**
  - `bot-service/src/db/usuarios.repository.ts` — `createUsuariosRepository`
    ahora recibe `(db, encryptionKey: Buffer)`; se agregan
    `guardarPatCifrado(chatId, pat)` (cifra y hace `UPDATE`, falla si el
    `chat_id` no existe — 0 filas afectadas) y `obtenerPatDescifrado(chatId)`
    (retorna `string | null`; `null` si no hay fila o `pat_cifrado` es
    `NULL`). AC #2, #3.
  - `bot-service/src/config/env.ts` — nuevo `patEncryptionKey: Buffer`,
    `required("PAT_ENCRYPTION_KEY")` decodificado y validado (32 bytes) por
    `decodificarClaveCifrado` en el momento de construir `env` — el proceso
    falla al importar el módulo (arranque) si falta o tiene el largo
    incorrecto. AC #4.
  - `bot-service/.env.example` — documenta `PAT_ENCRYPTION_KEY` (hex de 64
    caracteres, `openssl rand -hex 32`).
  - `bot-service/src/index.ts` y `bot-service/scripts/alta-usuario.ts` —
    actualizados para pasar `env.patEncryptionKey` al nuevo segundo parámetro
    de `createUsuariosRepository` (consecuencia obligatoria del cambio de
    firma, no una desviación de alcance en sí).
  - Tests existentes que instancian `UsuariosRepository` o llaman a
    `createUsuariosRepository` (`usuarios.repository.test.ts`,
    `usuarios.repository.integration.test.ts`, `fakeUsuariosRepository.ts`,
    `whitelistMiddleware.test.ts`, `provisioning/alta-usuario.test.ts`) —
    actualizados con una clave dummy de test y/o stubs de los métodos nuevos
    para que seguir satisfaciendo el tipo `UsuariosRepository`.

### Desviación de alcance documentada

`scripts/alta-usuario.ts` no está en el "Owned File/Module Scope" de esta
historia, pero su paso `completar-pat` (creado en 1.3) tenía un sink
placeholder explícito ("Historia 1.4 todavía no está implementada... se
descarta acá") pensado exactamente para ser reemplazado una vez que 1.4
existiera. Se reemplazó ese placeholder por un sink real que llama a
`usuariosRepository.guardarPatCifrado`, cerrando el ciclo completo de alta de
usuario (1.3 + 1.4) de punta a punta. Sin este cambio, el AC #2 de esta
historia ("dado un PAT nuevo, entregado por el flujo de alta de 1.3, cuando se
guarda...") no tendría ninguna forma real de ejercitarse en producción.

### Decisiones de diseño

- `guardarPatCifrado` falla explícitamente si el `UPDATE` afecta 0 filas (el
  `chat_id` no existe) — mismo criterio de "fail-fast ante uso incorrecto" que
  `usuariosRepository.crear` de 1.3, en vez de tolerar en silencio un PAT sin
  usuario asociado.
- `createUsuariosRepository` recibe la clave de cifrado como parámetro
  explícito (`Buffer`) en vez de leer `env` internamente — sigue el mismo
  patrón que `createFireflyAdminClient` de 1.3 (config explícita, no lectura
  de env oculta dentro del módulo) y mantiene el repositorio testeable sin
  necesidad de que los tests importen `config/env.ts` (que ya hace `required()`
  de varias variables al importarse).
- `pat-crypto.ts` es puro (no importa `env` ni loggea nada) — la validación de
  arranque (AC #4) vive en `config/env.ts`, que es el único lugar que ya tenía
  la responsabilidad de fallar rápido ante configuración inválida.

### Auditoría de logging (AC #5)

Revisados los puntos de logging existentes que tocan usuarios (1.1
`whitelistMiddleware.ts`, 1.3 `alta-usuario.ts`/`provisioning`): ninguno
loggea el objeto `usuario` completo ni el resultado de `findByChatId` — todos
loggean campos explícitos (`chat_id`, `autorizado`, `administrador`,
`firefly_user_id`). `findByChatId` tampoco selecciona `pat_cifrado` en su
`SELECT` (sigue pidiendo solo `chat_id, activo`), así que ni siquiera podría
filtrarse por ese camino. El único método que toca el PAT en texto plano
(`obtenerPatDescifrado`) no tiene todavía ningún caller en el código (queda
para 1.5) — no hay superficie de logging que auditar ahí más allá de lo que
1.5 deberá respetar cuando lo consuma. `pat-crypto.ts` no loggea nada.

### Testing local

- `npm test`: 67 passed, 5 skipped (2 de `usuarios.repository.integration.test.ts`
  de 1.1 + 3 de `usuarios.repository.pat.test.ts` — todas de integración con
  Postgres real vía contenedor efímero, saltadas por falta de Docker en este
  entorno de desarrollo; correrán completas en la VPS).
- `npx tsc --noEmit`: sin errores.

### Verificado en producción (2026-09-12)

El administrador aplicó la migración `0002`, configuró `PAT_ENCRYPTION_KEY` en
el `.env` de la VPS, reinició el Bot Service y confirmó que el flujo
end-to-end (`crear` → `completar-pat` → PAT cifrado y persistido en
Postgres) funciona correctamente contra la instancia real. AC #1, #2, #3, #4 y
#5 quedan verificados en producción.
