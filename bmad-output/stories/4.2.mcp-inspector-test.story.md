<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories
  File name: {epic}.{story}.{slug}.story.md  (e.g. 2.1.stripe-integration.story.md)

  This file is SELF-CONTAINED (~8K-token target): a dev agent should need nothing else.
  It is the LAST planning artifact before external dev handoff.

  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — they are the compiled, cited contract.
  Only this planning skill may change a LOCKED section, via an explicit Update + decision-log entry.

  The external dev tool fills ONLY the "Dev Agent Record" section.
-->

# 4.2: Probar el MCP de forma aislada con MCP Inspector

**Story ID:** 4.2
**Epic:** Epic 4 — Servidor MCP
**Slug:** mcp-inspector-test
**Status:** ready-for-dev   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **desarrollador**,
I want **probar el servidor MCP de Firefly III de forma aislada con MCP Inspector, incluyendo el envío del PAT por-request**,
so that **pueda confirmar que las tools responden correctamente y que el aislamiento por credencial funciona antes de conectar el MCP a Gemini (Epic 5), donde un fallo sería más difícil de diagnosticar**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. MCP Inspector (`npx @modelcontextprotocol/inspector`) se conecta exitosamente al servidor MCP desplegado en la historia 4.1, usando el modo de transporte configurado (stdio o HTTP según lo documentado en 4.1).
2. Desde MCP Inspector, se configura el PAT de un usuario de prueba en el header/campo correspondiente por-request (no como variable de entorno del propio Inspector), replicando cómo lo hará el Bot Service en Epic 5.
3. El listado de tools (`tools/list`) expuesto por el MCP se revisa y se documenta: qué tools existen, qué parámetros esperan, y cuáles corresponden a operaciones de lectura vs. escritura vs. potencialmente destructivas — este inventario alimenta directamente la historia 4.3.
4. Al menos una tool de lectura (ej. listar cuentas o transacciones) y una tool de escritura (ej. crear transacción) se ejecutan desde MCP Inspector contra la cuenta del usuario de prueba, confirmando que el flujo de invocación y la respuesta JSON-RPC son correctos.
5. Repetir la tool de lectura cambiando el PAT en Inspector por el de un segundo usuario de prueba confirma que la respuesta cambia de acuerdo al usuario (mismo control de aislamiento que 4.1, ahora validado a través de la herramienta que efectivamente ejercita el protocolo MCP completo, no solo `curl`).
6. Los hallazgos de esta sesión de prueba (tools disponibles, comportamiento ante errores, particularidades del transporte) quedan anotados en la documentación del servicio MCP para que Epic 5 y la historia 4.3 los reutilicen sin repetir la exploración.

<!-- Keep to ~3-7. More than ~7 means the story is too big — split it. -->

## Tasks / Subtasks

- [ ] Levantar MCP Inspector contra el servicio desplegado en 4.1 (AC: #1)
  - [ ] Ejecutar `npx @modelcontextprotocol/inspector` apuntando al MCP server desplegado
  - [ ] Confirmar conexión exitosa (handshake JSON-RPC inicial sin errores)
- [ ] Configurar el PAT por-request dentro de Inspector (AC: #2)
  - [ ] Ubicar en la UI/config de Inspector el campo de headers custom y setear `Authorization: Bearer <PAT>` con el PAT del usuario de prueba A
- [ ] Listar y documentar el catálogo de tools (AC: #3)
  - [ ] Ejecutar `tools/list` desde Inspector
  - [ ] Clasificar cada tool como lectura / escritura / potencialmente destructiva en la documentación del servicio MCP (insumo directo para 4.3)
- [ ] Ejecutar pruebas funcionales de al menos una tool de lectura y una de escritura (AC: #4)
  - [ ] Invocar una tool de lectura (ej. `list_accounts` o equivalente) y verificar la respuesta
  - [ ] Invocar una tool de escritura (ej. `create_transaction` o equivalente) y verificar que la transacción aparece en Firefly III
- [ ] Repetir la prueba de lectura con el PAT del usuario B (AC: #5)
  - [ ] Cambiar el header de Authorization en Inspector al PAT del usuario de prueba B
  - [ ] Confirmar que la respuesta corresponde solo a los datos de B
- [ ] Documentar hallazgos de la sesión (AC: #6)
  - [ ] Anotar en `mcp/README.md` (o equivalente creado en 4.1) el catálogo de tools, su clasificación, y cualquier comportamiento inesperado ante errores

## Dev Notes

<!-- LOCKED. Concrete guidance compiled from the planning docs WITH SOURCE CITATIONS.
     Cite every fact: [Source: prd.md#anchor] / [Source: architecture.md#anchor] /
     [Source: ux-design.md#anchor]. Label your own inferences [Inference]. -->

- Historia de backlog origen: "Como desarrollador, quiero probar el MCP de forma aislada con MCP Inspector, para confirmar que las tools responden antes de conectarlo a Gemini." [Source: docs/plan/backlog-scrum-completo.md#épica-4-servidor-mcp]
- El plan de implementación original ya identificaba este paso explícitamente: "Probarlo con MCP Inspector (`npx @modelcontextprotocol/inspector`) antes de conectarlo a nada más" — confirma tanto el comando exacto como la intención de aislar la prueba del resto del pipeline. [Source: docs/plan/backlog-plan-implementacion.md#épica-3-servidor-mcp]
- Esta prueba debe ejercitar el mismo mecanismo de credenciales por-request validado en 4.1 (PAT + URL de Firefly III como headers), porque es "la pieza que hace viable" el diseño multi-tenant completo — probarlo aquí con la herramienta estándar del protocolo (no solo `curl`) da mayor confianza de que Gemini, en Epic 5, invocará las tools de la misma forma. [Source: docs/plan/system-design-v2-multiusuario.md#3-la-pieza-que-hace-viable-la-opción-b-credenciales-por-request-en-el-mcp]
- Arquitectura de referencia para esta épica: `@firefly-iii-mcp/server` (o equivalente Python), protocolo JSON-RPC (stdio/HTTP), `@modelcontextprotocol/inspector`. [Source: docs/plan/system-design-v2-multiusuario.md#3-la-pieza-que-hace-viable-la-opción-b-credenciales-por-request-en-el-mcp]
- El catálogo de tools documentado aquí (AC #3) es el insumo directo de la historia 4.3 (`mcp-tools-restringidas`), que decide qué subconjunto de esas tools queda expuesto a Gemini — evitar duplicar la exploración del catálogo en 4.3. [Inference, basado en la secuencia ordenada de historias in bmad-output/epics.md#epic-4-servidor-mcp]
- Esta historia es explícitamente de aislamiento previo a Gemini: "Out of scope: integración con Gemini (Epic 5)" para toda la épica — MCP Inspector reemplaza a Gemini como cliente de prueba en esta fase. [Source: bmad-output/epics.md#epic-4-servidor-mcp]
- Firefly III ya aísla datos por `user_id` en cada endpoint de su API — el objetivo de las pruebas con dos PATs no es descubrir un bug en Firefly III, sino confirmar que el MCP reenvía correctamente el header recibido sin cachearlo ni mezclarlo entre sesiones de Inspector. [Source: docs/plan/system-design-v2-multiusuario.md#2-decisión-de-arquitectura-clave-¿un-firefly-iii-por-usuario-o-uno-solo-para-todos]

## Testing

<!-- LOCKED. STRATEGY ONLY — what to verify and how. NEVER run tests or quote coverage here. -->

- **Approach:** Prueba exploratoria/manual guiada con MCP Inspector como cliente de protocolo real, contra el MCP desplegado en 4.1 y una instancia Firefly III de desarrollo. No es testing automatizado de código propio — es validación de comportamiento de un componente de terceros bajo el contrato esperado (headers por-request, catálogo de tools).
- **Key scenarios:** (a) Inspector se conecta y completa el handshake; (b) `tools/list` devuelve un catálogo no vacío y coherente con la documentación de Firefly III; (c) una tool de lectura y una de escritura ejecutan correctamente con el PAT del usuario A; (d) la misma tool de lectura, con el PAT del usuario B, devuelve datos distintos; (e) una tool invocada con parámetros inválidos devuelve un error JSON-RPC legible, no un crash del servidor.
- **Per-AC verification:** AC #1 → captura de la conexión exitosa en Inspector; AC #2 → confirmación visual en Inspector de que el header `Authorization` está seteado por request y no por entorno; AC #3 → tabla de tools documentada (nombre, tipo de operación, parámetros); AC #4 → comparación de la respuesta de la tool de escritura contra el estado real en Firefly III (vía UI de Firefly o la propia tool de lectura); AC #5 → comparación lado a lado de las dos respuestas (usuario A vs. usuario B) confirmando que no coinciden; AC #6 → revisión de que la documentación del servicio MCP quedó actualizada con el catálogo y los hallazgos.
- **Fixtures / mocks:** los mismos dos PATs de prueba usados en 4.1 (o nuevos si los de 4.1 expiraron/fueron rotados); no se mockea nada — se prueba contra el MCP y Firefly III reales de desarrollo.
- **Edge cases to cover:** tool inexistente invocada por nombre incorrecto; parámetros faltantes o de tipo incorrecto en una tool de escritura; PAT expirado o revocado a mitad de sesión de Inspector; tool de escritura invocada dos veces seguidas (confirmar que no hay duplicación silenciosa de transacciones).

## Dependency Maps

- **Blocked by:** 4.1 (`mcp-multitenant-deploy`) — el MCP debe estar desplegado y aceptando credenciales por-request antes de poder probarlo con Inspector. También hereda la dependencia de Epic 4 sobre Epic 1 (PAT por-request) y Epic 3 (conectividad Firefly III validada). [Source: bmad-output/epics.md#epic-4-servidor-mcp]
- **Blocks:** 4.3 (`mcp-tools-restringidas`) — necesita el catálogo de tools documentado aquí para decidir qué restringir.
- **External:** `@modelcontextprotocol/inspector` (vía `npx`); el servicio MCP desplegado en 4.1; instancia Firefly III de desarrollo; dos PATs de prueba.

## Owned File/Module Scope

<!-- EXPLICIT list of paths this story may create or modify. This is the lever for
     parallel-conflict-free scheduling: disjoint scopes can run in parallel. Be path-precise,
     prefer files over broad globs, include test paths, and call out shared/contended files.
     Validated by ${CLAUDE_PLUGIN_ROOT}/scripts/scope-conflict-check.sh. -->

- `mcp/README.md` (creado en 4.1 — se amplía con el catálogo de tools y hallazgos de la sesión de Inspector) — Shared/contended: también lo toca 4.1 (creación inicial) y 4.3 (documenta el set restringido final)
- `mcp/tools-inventory.md` (opcional, si el catálogo de tools amerita un documento propio separado del README)

## Learnings from Previous Stories

<!-- Carried forward from completed sibling stories: chosen patterns, gotchas, helpers
     created, naming conventions. Only what is actionable for THIS story. -->

- De 4.1 (una vez completada): el modo de transporte elegido (stdio vs. HTTP) y la forma exacta de pasar el PAT por header — reutilizar esa misma configuración al conectar MCP Inspector, no reinventarla. (from 4.1)
- Ninguna otra historia previa completada aún en este track.

## Dev Agent Record

<!-- LEFT EMPTY by planning. The external dev tool fills this in during implementation. -->

_(empty — to be completed by the external dev tool)_
