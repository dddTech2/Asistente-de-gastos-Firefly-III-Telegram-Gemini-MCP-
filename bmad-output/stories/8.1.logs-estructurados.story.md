<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories
  File name: {epic}.{story}.{slug}.story.md  (e.g. 2.1.stripe-integration.story.md)

  This file is SELF-CONTAINED (~8K-token target): a dev agent should need nothing else.
  It is the LAST planning artifact before external dev handoff.

  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — they are the compiled, cited contract.
  Only this planning skill may change a LOCKED section, via an explicit Update + decision-log entry.

  The external dev tool fills ONLY the "Dev Agent Record" section.
-->

# 8.1: Logs estructurados

**Story ID:** 8.1
**Epic:** 8 — Observabilidad y hardening
**Slug:** logs-estructurados
**Status:** done   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As an **administrador del sistema (el propio desarrollador solo-dev)**,
I want **logs estructurados (JSON) de todo lo que hace el Bot Service — mensajes entrantes, llamadas a Gemini, llamadas a tools del MCP, errores**,
so that **pueda depurar problemas rápido sin tener que adivinar qué pasó en producción**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. El Bot Service emite logs en formato estructurado (JSON, un objeto por línea) en vez de `console.log` de texto libre, para todo evento relevante: webhook recibido, mensaje encolado/desencolado, llamada a Gemini (inicio/fin/error), llamada a tool del MCP (inicio/fin/error), y cualquier excepción no controlada.
2. Cada línea de log incluye como mínimo: `timestamp`, `level`, `chat_id` (cuando aplica), `update_id` de Telegram (cuando aplica, para correlacionar con la deduplicación de HU-27), y un `message`/`event` descriptivo.
3. El nivel de log (`debug`/`info`/`warn`/`error`) es configurable vía variable de entorno (`LOG_LEVEL`), sin requerir cambios de código ni redeploy.
4. Ningún log contiene el PAT de Firefly III de un usuario, la API key de Gemini, el token del bot de Telegram, ni cualquier otro secreto en texto plano — se redactan o se omiten explícitamente antes de escribir el log.
5. Los logs se emiten a `stdout`/`stderr` (no a un archivo propio), de forma que Docker los capture con el driver de logging estándar, sin infraestructura adicional (nada de ELK/Loki — ver Dev Notes sobre alcance).

<!-- Keep to ~3-7. More than ~7 means the story is too big — split it. -->

## Tasks / Subtasks

- [ ] Elegir e instalar la librería de logging estructurado para el Bot Service (Node.js + TypeScript) (AC: #1, #3)
  - [ ] Evaluar `pino` (preferido por overhead bajo) vs. alternativas; documentar la elección con un comentario breve en `src/lib/logger.ts`
  - [ ] Configurar el logger para leer `LOG_LEVEL` desde `process.env` con un default seguro (`info`)
- [ ] Implementar redacción de secretos (AC: #4)
  - [ ] Configurar la lista de paths/campos a redactar (`pat`, `token`, `authorization`, `apiKey`, etc.) usando la opción de redacción nativa del logger elegido
  - [ ] Escribir un test unitario que confirme que un objeto con un campo `pat` no aparece en texto plano en la salida
- [ ] Instrumentar los puntos de entrada/salida clave con logging estructurado (AC: #1, #2)
  - [ ] Handler del webhook de Telegram: log al recibir (`update_id`, `chat_id`) y al responder
  - [ ] Cliente MCP: log antes y después de cada `tool_call`, incluyendo nombre de la tool y resultado (éxito/error), sin loguear el PAT usado en el header
  - [ ] Cliente de Gemini: log de inicio/fin de cada llamada, incluyendo duración
  - [ ] Manejador global de errores no controlados: log con `level: error` y stack trace
- [ ] Verificar que los logs van a stdout/stderr sin configuración adicional de Docker (AC: #5) (AC: #5)

## Dev Notes

<!-- LOCKED. Concrete guidance compiled from the planning docs WITH SOURCE CITATIONS.
     Cite every fact: [Source: prd.md#anchor] / [Source: architecture.md#anchor] /
     [Source: ux-design.md#anchor]. Label your own inferences [Inference]. -->

- Requisito original: "Como administrador, quiero logs estructurados de todo lo que hace el bot, para poder depurar problemas rápido" (HU-30). [Source: docs/plan/backlog-scrum-completo.md#épica-8-observabilidad-y-hardening]
- El Bot Service es Node.js + TypeScript (con grammY o Telegraf para Telegram); es el stack sobre el que corre este logger. [Source: docs/plan/backlog-plan-implementacion.md#1-stack-tecnológico-recomendado]
- Alcance explícitamente acotado: "Monitoreo: Uptime Kuma + logs es suficiente. Un stack tipo Prometheus/Grafana sería sobre-ingeniería para este tamaño." Esta historia NO incluye un agregador de logs centralizado (ELK/Loki/Grafana) — solo logs estructurados a stdout que Docker ya captura. [Source: docs/plan/system-design-asistente-gastos.md#4-scale-and-reliability]
- Distinción importante con la historia 8.2: esta historia (8.1) cubre logs **operativos/técnicos** para debugging (qué falló, cuánto tardó). La tabla `log_auditoria_ia` de la historia 8.2 es un registro de negocio distinto — auditoría de qué escribió el usuario y qué tool ejecutó la IA, persistida en base de datos, no en logs de proceso. No deben confundirse ni duplicar el mismo mecanismo. [Source: docs/plan/system-design-asistente-gastos.md#3-deep-dive]
- Requisito de idempotencia ya presente en el diseño: se usa el `update_id` de Telegram para descartar webhooks duplicados — por eso el `update_id` debe viajar en los logs, para poder correlacionar reintentos de Telegram con lo que el bot realmente procesó. [Source: docs/plan/system-design-asistente-gastos.md#3-deep-dive]
- Definition of Done del proyecto exige explícitamente que si una historia "toca datos sensibles (PAT, credenciales), quedaron cifrados/fuera de logs" — esto es la base directa del AC #4. [Source: docs/plan/backlog-scrum-completo.md#4-definition-of-done-dod]
- La elección concreta de librería (`pino` como sugerencia) no está en ningún documento de planeación — es una decisión técnica razonable dado el stack Node/TS y no bloquea al dev tool si elige otra librería equivalente que cumpla los ACs. [Inference]

## Testing

<!-- LOCKED. STRATEGY ONLY — what to verify and how. NEVER run tests or quote coverage here. -->

- **Approach:** unit tests para el logger y su configuración de redacción; test de integración ligero que ejercite el webhook handler y capture la salida de log para verificar forma/campos.
- **Key scenarios:** log de un webhook entrante válido; log de un error no controlado; log de una llamada a tool del MCP con un parámetro que contiene un PAT simulado (debe salir redactado); cambio de `LOG_LEVEL` vía variable de entorno afecta qué niveles se emiten.
- **Per-AC verification:** AC #1 → inspección de la salida de log tras simular cada evento (webhook, Gemini, tool MCP, error) y confirmar que es JSON parseable; AC #2 → aserciones sobre las claves presentes (`timestamp`, `level`, `chat_id`, `update_id`, `message`); AC #3 → test parametrizado que instancia el logger con distintos `LOG_LEVEL` y confirma qué niveles pasan; AC #4 → test de redacción con un payload que incluye `pat`/`token`/`apiKey` y assert de que el valor no aparece en texto plano en la salida serializada; AC #5 → verificación manual/documental de que no se escribe a ningún archivo (el dev tool no necesita levantar Docker para esto, solo confirmar que el logger apunta a stdout/stderr).
- **Fixtures / mocks:** payload de webhook de Telegram de ejemplo (`update_id`, `chat_id`, texto); respuesta simulada de una tool call del MCP con un header `Authorization` de ejemplo para probar la redacción; mock del cliente de Gemini para simular duración de llamada.
- **Edge cases to cover:** `chat_id`/`update_id` ausentes (ej. un error que ocurre antes de parsear el webhook) no deben romper el logger; un objeto de error sin `message` ni `stack`; un `LOG_LEVEL` inválido en la variable de entorno debe caer a un default seguro en vez de crashear el proceso al arrancar.

## Dependency Maps

- **Blocked by:** Epic 0 (necesita el Bot Service desplegable vía Docker Compose para que stdout llegue a algún lado) — dependencia transversal, no bloqueante para escribir el código del logger. [Source: bmad-output/epics.md#epic-8-observabilidad-y-hardening]
- **Blocks:** 8.2 (tabla-auditoria-ia) se beneficia de tener ya un logger central configurado para loguear también errores de escritura en la tabla de auditoría, aunque no es un bloqueo duro — ambas pueden desarrollarse en paralelo.
- **External:** ninguna dependencia de servicio externo; solo la librería de logging (ej. `pino`) como dependencia de npm.

## Owned File/Module Scope

<!-- EXPLICIT list of paths this story may create or modify. This is the lever for
     parallel-conflict-free scheduling: disjoint scopes can run in parallel. Be path-precise,
     prefer files over broad globs, include test paths, and call out shared/contended files.
     Validated by ${CLAUDE_PLUGIN_ROOT}/scripts/scope-conflict-check.sh. -->

- `src/lib/logger.ts` (nuevo — configuración central de pino/logger elegido, incluida redacción)
- `src/config/logging.config.ts` (nuevo — parseo de `LOG_LEVEL` desde env)
- `tests/unit/lib/logger.test.ts` (nuevo)
- `tests/integration/webhook-logging.test.ts` (nuevo)
- `bot-service/.env.example` — agregar `LOG_LEVEL=info` (Shared/contended: Epic 0 y Epic 1 también agregan variables acá)
- Shared/contended (instrumentados por esta historia pero creados/poseídos por otras épicas): `src/bot/webhookHandler.ts` (Epic 2/6), `src/services/mcpClient.ts` (Epic 4), `src/services/geminiService.ts` (Epic 5) — esta historia solo agrega llamadas al logger dentro de ellos, no los crea.

## Learnings from Previous Stories

<!-- Carried forward from completed sibling stories: chosen patterns, gotchas, helpers
     created, naming conventions. Only what is actionable for THIS story. -->

- Ninguna — 8.1 es la primera historia compilada de la Épica 8, no hay historias previas completadas de las que heredar patrones.

## Dev Agent Record

<!-- LEFT EMPTY by planning. The external dev tool fills this in during implementation. -->

**Adaptación de rutas de archivo:** el Owned File/Module Scope de esta historia usa rutas genéricas (`src/lib/logger.ts`, `src/services/mcpClient.ts`, etc.) escritas antes de que existiera la estructura real del repo. El código del bot vive en `bot-service/`, así que los archivos nuevos se crearon en `bot-service/src/lib/logger.ts` y `bot-service/src/config/logging.config.ts` — incluidas en el paquete que ya despliega el Bot Service, no en un `src/` de la raíz que no existe. [Inference]

**Librería elegida:** `pino` (sugerencia del Dev Notes), por overhead bajo en el hot path del webhook. Se configuró con `formatters.level` (para que el campo sea el string `"info"/"error"/...` en vez del número interno de pino) y `timestamp` custom (ISO 8601 bajo la clave `timestamp`, en vez del epoch-ms `time` por defecto) para calzar exactamente con los nombres de campo del AC #2. El campo de mensaje usa el `msg` nativo de pino — cumple "message/event descriptivo" del AC sin pelear contra el default de la librería.

**Redacción (AC #4):** `redact.paths` de pino (motor `fast-redact`) cubre `pat`, `token`, `apiKey`, `authorization` en el nivel top-level y un nivel de anidamiento (`*.pat`, etc.) más `headers.authorization` explícito — suficiente porque los call sites de logging son controlados por este mismo código (no se loguean objetos arbitrarios de terceros sin pasar por estas claves conocidas). Verificado con test que confirma que el valor original no aparece en el JSON serializado, solo `"[REDACTED]"`.

**`LOG_LEVEL` configurable (AC #3):** `src/config/logging.config.ts` expone `resolveLogLevel` (pura, testeada) que valida contra `debug|info|warn|error` y cae a `info` ante cualquier valor ausente o inválido — nunca rompe el arranque del proceso. `logger.ts` expone además `createLogger(level, destination?)` (no solo el singleton) específicamente para poder testear distintos niveles y capturar la salida sin pegarle a `stdout` real en los tests.

**Instrumentación (AC #1, #2) — solo de lo que existe hoy en el código:**
- `telegramWebhook.ts`: log `info` al recibir el webhook (`update_id`, `chat_id` extraídos con una función defensiva `extractLogContext` que soporta `message`/`edited_message`/`channel_post`/`edited_channel_post`/`callback_query.message` y no rompe si el update no matchea ninguno), log `debug` al encolar, log `info`/`debug` al desencolar y terminar de procesar, log `error` (con `err` serializado vía `pino.stdSerializers.err`, stack incluido) si `bot.handleUpdate` falla — reemplaza el logger mínimo de la historia 2.3 (`src/logging/logger.ts`, eliminado — su propio Dev Agent Record ya anticipaba que sería superado acá).
- `index.ts`: `process.on("uncaughtException"/"unhandledRejection")` loggean con `level: error` y stack trace antes de dejar que el proceso siga (no se llama `process.exit()` — pm2 ya maneja reinicios si hiciera falta) (AC #1, "excepción no controlada").
- Cliente Gemini y cliente MCP (Tasks originales): **no instrumentados** — esos módulos todavía no existen en el código (son Epic 4 y Epic 5, no implementadas). Cuando se construyan, deben usar `logger` de `src/lib/logger.ts` para las llamadas de inicio/fin/error, siguiendo el mismo patrón que `telegramWebhook.ts`. [Inference — no se puede instrumentar código que no existe]

**Verificado localmente:** `npm run build` limpio; `npm test` → 29/29 en verde (14 previos + 6 de `logging.config.test.ts` + 5 de `logger.test.ts` + 4 de `webhookLogging.integration.test.ts`, más el test de AC #4 de `telegramWebhook.integration.test.ts` actualizado para espiar el `logger` nuevo en vez del logger mínimo de 2.3). `npm audit`: pino no agrega vulnerabilidades nuevas (las 7 preexistentes son dev-only de vitest/vite/esbuild y el `qs` transitivo de `express` ya triagado en la historia 2.1).

**Verificado en producción (2026-09-12):** usuario desplegó (`git pull && npm install && npm run build && pm2 restart bot-service`) y confirmó vía `pm2 logs bot-service` que la salida real es JSON estructurado (AC #5) — logs correlacionables por `update_id`/`chat_id` visibles tal como se diseñaron.

**Status:** done.
